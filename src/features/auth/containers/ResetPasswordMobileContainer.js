import React from 'react';

import ResetPasswordContainer from './ResetPasswordContainer';

const ResetPasswordMobileContainer = props => (
  <ResetPasswordContainer {...props} isResetMobile />
);

export default ResetPasswordMobileContainer;